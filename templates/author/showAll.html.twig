<table>
<tr>
   <td>Username</td>
   <td>Email</td>
   <td>Actions</td>
</tr>
{% for i in list %}
<tr>
<td>{{ i.username}}</td>
<td>{{i.email}} </td>
<td>
 <a href="{{path('deleteAuthor',{'id':i.id})}}">delete</a> {# path or url  #}
 <a href="{{path('showAuthorDetails',{'id': i.id})}}">show</a>
 <a href="{{ path('updateForm', {'id': i.id}) }}">Update</a>

 
</tr>
{% endfor %}
</table>